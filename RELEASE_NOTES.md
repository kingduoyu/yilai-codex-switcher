# v3.3.12：连接配置优先，异常历史不再阻断

- API 和官方连接配置先独立完成；本地历史检查失败或部分跳过时，不再回滚已经生效的配置、模型目录和登录处理。
- 取消重复 session ID 的全局拒绝。同一 ID 出现在多个 JSONL 时逐文件处理，不要求客户手工整理后才能配置。
- 损坏、无法读取、备份失败或写入失败的单个历史文件记录告警并跳过，其余有效会话继续迁移。
- 已成功迁移的 `yilai` 会话仍保留备份、恢复和重复运行幂等；已是 `custom` 或其他来源的会话不改写。
- 保留 v3.3.11 的默认权限处理、模型列表、快速重复检查和两个稳定下载文件名。

先退出 Codex/CCS，再点击所需切换按钮。登录策略未改变：API 配置仍删除 auth.json，官方切换保留现有文件，不自动恢复已删除的登录；缺少登录时需重新登录。本版不修改 CCS 数据库，也不解决其 current 与外部配置的回存冲突。未验证生产官方账号发送或付费生图。Mac 临时签名、未公证。
