# v3.3.7 — 本地快速配置与固定模型目录

- 主按钮不再启动 Codex 检查配置来源或回读验证，只进行本地配置写入。
- 自动写入固定的 gpt-5.6-sol、gpt-5.6-terra、gpt-6-astra 模型目录，并设置根配置及当前 profile 的目录指向。
- 已选模型属于上述列表时保留，否则默认 sol；不联网刷新模型列表。
- 目录、配置和认证删除失败时恢复本次改动，保留错误日志。Windows 显示操作阶段及用时。
- 安装包文件名携带 v3.3.7，应用窗口和日志同步版本号。

重置仍为改名停用当前 config.toml；不处理其他来源优先级，不迁移历史，不修改 CCS 数据库。使用 CCS 时先选 API 供应商再退出并配置，外部回存到官方条目的冲突未自动修复。macOS 通用版临时签名、未公证。
