# v3.3.17：独立旧对话修复与登录文件回收

- 切换 API 时，将旧 auth.json 移入 Windows 回收站或 macOS 废纸篓，不再直接删除。
- 不生成固定名称的登录备份，已有 auth.json.yilai-disabled 不阻止切换。
- 最终核验告警不回滚已完成的连接配置。
- 常规配置和切换不处理历史；新增独立“修复旧易来对话”按钮，仅手动修复旧 yilai 归属。
- 重复 ID、坏文件和索引异常逐项跳过，已成功的修复保留，可再次手动补齐未完成项。
- 教程固定下载地址保持不变。
