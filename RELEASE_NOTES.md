# v3.3.10：官方切换安全修复

- 官方切换拒绝含 NUL 的异常配置，避免静默丢失后半段内容。
- 写入和回滚前核验配置状态，检测到外部修改时不覆盖；回滚失败保留原始原因并明确提示未恢复。
- 保留未选中 profile 的原连接，覆盖 custom 和旧 yilai 引用；已有备份别名保留，重复切换不反复新增别名。
- 当前 custom/yilai 官方路由不带第三方地址、Key 或 Headers。API、官方、重置三个原有操作及固定模型目录保持不变。
- 历史仍只修复旧 yilai 的 JSONL/SQLite 归属，不修改其他来源、正文、标题、归档或父对话元数据，保留备份与恢复。

先退出 Codex/CCS，直接点击所需切换按钮。登录策略未改变：API 配置仍删除 auth.json，官方切换保留现有文件，不自动恢复已删除的登录；缺少登录时需重新登录。本版不修改 CCS 数据库，也不解决其 current 与外部配置的回存冲突。未验证生产官方账号发送或付费生图。Mac 临时签名、未公证。
