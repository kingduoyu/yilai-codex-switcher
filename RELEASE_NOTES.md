# v3.3.5 — 专注 API 与生图

- 移除 Windows/macOS 的“切回官方”按钮及执行逻辑，官方切换交给 CCS。
- 主界面保留一个 API 配置按钮；重置仅改名停用当前配置，不恢复任何停用文件。
- 保持 CCS 已统一的 custom 历史归属，不修改 CCS 历史开关或迁移完成标记。
- 修复合法分叉对话含多条 session_meta 时的兼容问题，以第一条为当前会话元数据，保留继承的父会话记录。
- 历史同步或恢复异常改为单独警告，保留已成功的 API 配置并提供日志入口。配置写入、认证删除和来源核验错误仍阻止成功并回滚本次配置改动。

延续生图、实际来源识别和改名重置。测试使用本地模拟服务，不包含真实付费出图或真实官方登录。版本事实见 releases/v3.3.5/RELEASE-MANIFEST.md。
