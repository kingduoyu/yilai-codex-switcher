# v3.3.11：保留默认权限并加速历史检查

- 配置未显式写权限时，读取 Codex 桌面端已保存的模式；用户选择“完全访问”时写入 `approval_policy=never` 与 `sandbox_mode=danger-full-access`，避免切换后退回审批模式。
- 已显式配置审批或沙箱策略时保持原值，不自动提升权限。
- 历史检查先读取会话元数据前缀，只有旧 `yilai` 文件才读取并校验完整 JSONL；数据库先查询待迁移行，重复运行不再完整扫描全部内容。
- 仍只把旧 `yilai` 归属改为 `custom`，其他来源、正文、标题、归档和父对话元数据不修改，备份、失败恢复及完整候选校验保留。
- 发布页只保留 Windows EXE 与 macOS DMG 两个程序，使用稳定文件名，`releases/latest/download` 固定直链不再返回 404。

先退出 Codex/CCS，再点击所需切换按钮。登录策略未改变：API 配置仍删除 auth.json，官方切换保留现有文件，不自动恢复已删除的登录；缺少登录时需重新登录。本版不修改 CCS 数据库，也不解决其 current 与外部配置的回存冲突。未验证生产官方账号发送或付费生图。Mac 临时签名、未公证。
