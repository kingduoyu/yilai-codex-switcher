# v3.3.14：修复最终配置核验超时

- 临时数据库先在空历史目录完成初始化，避免最终配置核验触发真实历史索引重建。
- 多个活动项目复用一次配置读取会话，整轮核验共用 20 秒上限，不再按项目重复启动。
- 隔离核验进程的启动目录；超时提示明确区分运行时初始化和配置读取阶段，并显示运行时及配置上下文。
- 继续保留已完成的 API 配置；探测失败、功能冲突和历史异常只提示，不回滚连接。

下载地址保持不变，教程无需更新。此修复不改变 API、模型或生图配置规则。macOS 为 ad-hoc 签名、未公证。
