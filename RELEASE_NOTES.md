# v3.3.0 — 原生重写 / CCS 配置增强与本地历史统一

- Windows 与 macOS 从独立模块重新实现，默认启用生图不改模型或登录。
- 显式配置连接与旧登录清理分离。
- 新增全部本地历史同步、自动备份与撤销；保留新消息、标题和归档状态。
- 跟随 CODEX_HOME，历史索引支持 sqlite_home / CODEX_SQLITE_HOME。
- 切换官方由 CCS 完成；持续统一请开启 CCS 的统一会话历史。

候选验证进行中，正式制品与校验值以发布清单为准。
