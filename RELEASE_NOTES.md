# v3.3.8 — 本地历史统一与双向切换

- 增加“统一本地历史”：将已知旧 yilai、ccswitch、官方 openai 等 JSONL 元数据和 SQLite 索引统一到 custom，保留正文、标题、归档和父对话元数据。
- 提供备份、失败恢复和幂等处理；独立入口只在需要时扫描，普通切换保持本地快速写入。
- 恢复官方按钮。API/官方均使用 custom 归属；yilai 兼容别名跟随连接切换。官方配置不残留第三方地址或 Key。
- 保留固定的 sol、terra、astra 模型目录及指向。

使用方法：退出 Codex 和 CCS，点一次“统一本地历史”，完成后选 API 或官方，再打开 Codex。官方按钮保留当前 auth.json；若此前 API 切换已移除它，需要重新登录官方。首次迁移耗时取决于历史大小，不承诺十秒内。

采用 CCS 的统一归属与官方 custom 路由原则；未复制 CCS 账号管理、自动迁移设置或其数据库，不宣称所有功能一模一样。测试覆盖真实运行时的旧对话迁移后读取和模拟 API 续聊，未测试真实官方付费/订阅发送。Mac 为临时签名、未公证。
