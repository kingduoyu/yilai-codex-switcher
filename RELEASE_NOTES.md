# v3.3.6 — 精简为 API 配置与生图

- Windows/macOS 移除全部历史同步、迁移、撤销、恢复代码及 SQLite 构建依赖。
- 保留一个 API 配置主按钮，自动启用生图；辅助功能为改名重置配置和错误日志。
- 配置探测的数据库写入隔离到临时目录，不接触用户历史数据库。
- 保留现有模型选择、模型目录及其指向，包括旧版配置器目录；不新增模型获取或目录写入功能。
- 官方切换由 CCS 负责，本配置器不包含本次人工重置官方登录的操作。

使用 CCS 时，先选中 API 供应商再退出并运行配置器，避免 CCS 将外部改动回存到官方条目。该状态冲突尚未自动解决。

验证使用隔离配置和模拟 Responses 服务，不包含真实付费出图或官方登录。macOS 为通用架构、临时签名，未公证。版本事实见 releases/v3.3.6/RELEASE-MANIFEST.md。
