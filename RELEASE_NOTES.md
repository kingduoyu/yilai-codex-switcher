# v3.3.2 — 切换恢复与错误反馈优化

- 修复 Windows 切换失败后“查看日志”显示但不可点击的问题，并加入真实失败界面回归测试。
- 两端切换前自动处理上次中断的历史同步：完成的事务收尾，未完成的事务安全恢复后继续；保留新增消息和当前连接配置。
- 修复重复切换持续增加备用连接的问题；相同配置保持幂等，确有其他 profile 引用时才保留旧连接。
- 同一配置目录增加完整操作互斥，覆盖切换、重置和内部维护操作，避免多个配置器同时写入。
- Windows 文件操作错误附带系统错误码，macOS 错误保留原生错误域和编号；回滚检测到外部改动时保留现场并报错。
- 日志无法完整保存时明确说明，日志成败不改变实际切换结果。

仍保留两个主要切换按钮，生图设置和本地历史同步自动完成。“重置配置”仅改名 config.toml，不处理登录或历史，也不弹确认。

请先退出 Codex 和 CC-Switch。后续使用 CCS 切换时，开启它的统一 Codex 会话历史。历史同步只覆盖同一 CODEX_HOME 的本地记录。实际付费出图和真实账号登录不包含在模拟服务测试中。

双端最终验证、安装包校验与发布事实见 releases/v3.3.2/RELEASE-MANIFEST.md。
