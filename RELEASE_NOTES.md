# v3.3.0 — 原生重写 / CCS 配置增强与本地历史统一

- Windows 与 macOS 从独立模块重新实现：配置规则、历史同步核心、平台文件操作与界面分离。
- 默认“启用生图”保留 CCS 的模型、目录、登录及其他配置；不再生成固定模型目录。
- “配置易来连接”与“清理旧登录”分离，清理仅在明确操作后执行，并使用 Windows 回收站 / macOS 废纸篓。
- 新增全部本地历史同步、备份与撤销，保留新增消息、标题和归档状态；保留其他 profile 的原连接。
- 修复 Windows 中文路径；macOS 保留原生废纸篓、文件锁定处理，并保护操作中的正常退出。

使用 CCS 切回官方；要在之后切换中持续统一分类，请开启 CCS 的“统一 Codex 会话历史”。本工具不修改 CCS。同步范围是同一 CODEX_HOME 中已有的本地历史，不下载其他账号的云端会话。

验证：Windows 原生自测通过；Codex app-server 0.153.4 隔离集成通过，覆盖模型列表、内置生图工具与请求头、真实对话创建/读取/继续、同步及撤销、官方模式及模拟登录凭据更换。生产服务实际生成图片由用户使用时验收，测试没有调用付费 API。最终 macOS 构建、自测和制品校验见发布清单。
