# v3.3.3 — 识别并解除当前连接覆盖

- 两个主切换先通过已安装 Codex 读取实际配置层及字段来源，再处理覆盖，不按文件名猜测。
- 对默认用户配置之上、当前活动项目中已加载的连接覆盖，先备份，再移除相关字段；保留模型、目录、MCP、权限和无关设置。
- 写入易来/官方连接后再次读取实际值，核验地址、认证和 API 生图设置；未生效则明确失败并恢复来源、配置和登录改动。
- 未受信任、未加载的项目配置不处理，未选中的独立 profile 不扫描。来源备份和中断恢复纳入操作日志。
- 延续两个主要按钮、自动历史同步、登录文件删除和改名重置；重置仅处理当前 config.toml，不清理项目来源。

检测范围为本机默认启动，以及桌面记录的当前本机活动项目。独立 CLI --profile/-c、未来打开的新项目和后续启动参数变更不在已核验范围；当前 app-server 无法选择 CLI 独立 profile，不声称全局永久最高优先级。

未知来源或无法读取配置时会停止并报错；不会清除系统/组织策略。请先退出 Codex 和 CC-Switch。验证只调用本地配置读取和模拟服务，不包含实际付费出图或真实账号登录。

最终验证、校验值与发布事实见 releases/v3.3.3/RELEASE-MANIFEST.md。
