# v3.3.4 — 修复旧 Codex 运行时误选

- 修复 Windows 同时存在桌面端版本目录与旧版 bin/codex.exe 时，误选旧运行时导致 config/read 拒绝的问题。优先使用桌面端版本目录中的运行时，旧路径仅作为后备。
- 兼容当前运行时支持的 max 推理档位，保留用户原有模型和推理配置。
- 配置读取失败时显示 RPC 错误码、运行时路径、配置上下文和可识别的失败类别，不输出可能包含密钥的原始错误内容。
- 增加自动发现路径的回归验证，覆盖旧版遗留文件与新版并存；延续来源识别、连接覆盖处理和历史同步。

Windows 与 macOS 均更新到 3.3.4；macOS 保持其独立的应用包运行时查找方式。检测范围仍为默认配置与当前活动本地项目，不包含任意独立 CLI --profile/-c。验证不调用付费模型。

最终验证和制品校验值见 releases/v3.3.4/RELEASE-MANIFEST.md。
