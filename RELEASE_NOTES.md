# v3.3.9 — 切换时修复旧易来对话

- 不新增按钮；API 和官方切换完成前自动把旧易来 yilai 对话的 JSONL 元数据、SQLite 索引归属修正为 custom。
- 只处理 yilai；openai、ccswitch 和其他来源不迁移，不修改正文、标题、归档或父对话元数据。
- 已经是 custom 的不再改写。备份与失败恢复保留；首次扫描时间取决于历史量。
- API 与官方均使用 custom，yilai 兼容定义随当前路由更新。官方路由不残留第三方地址或 Key。

先退出 Codex/CCS，直接点击所需切换按钮。官方登录文件不存在时需要重新登录。未验证生产官方账号发送，不能将可加载旧对话等同于官方发送已通过。Mac 临时签名、未公证。
