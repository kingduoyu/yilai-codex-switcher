# v3.3.13：配置保留，直接提示缺失功能和冲突来源

- API 连接、模型目录和登录处理先作为核心事务完成；最终探测失败、功能不全或本地历史异常不再回滚已经可用的连接。
- 配置完成后用隔离 Codex app-server 回读最终有效配置，核对连接、API 地址、认证、三个模型目录、生图开关和生图授权。
- 项目配置、profile、启动参数、系统管理配置或 CCS 自定义 Codex 目录造成覆盖时，界面直接显示具体问题和可识别来源，便于客户截图反馈。
- 历史同步继续按文件尽量执行；损坏、重复、无法读取、备份或写入失败的个别会话跳过，不阻断 API 配置。
- 移除过程 JSON 日志和“查看日志”入口。错误与警告只在界面脱敏显示，不创建日志目录。
- 保留默认权限设置，不将完全访问降级为审批；公开 Release 仍只提供 Windows EXE 和 macOS DMG 两个安装文件。

只有连接配置、模型目录或登录文件在核心写入阶段形成半套状态时，才恢复本轮核心改动。最终探测和历史检查均为提交后的只读或尽力处理。本版不自动改写项目/profile/启动参数，也不修改 CCS 数据库。未验证生产官方账号发送或付费生图；macOS 为 ad-hoc 签名、未公证。
